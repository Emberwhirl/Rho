---
title: "Iris Summary (Quarto)"
format: html
---

# Iris Dataset Summary

A minimal Quarto document for verifying `.qmd` render in Rho.

```{r}
data(iris)
cat("Rows:", nrow(iris), "Cols:", ncol(iris), "\n")
```

```{r}
#| fig-width: 8
#| fig-height: 5
plot(iris$Petal.Length, iris$Petal.Width,
     col = as.integer(iris$Species),
     pch = 16,
     xlab = "Petal Length (cm)",
     ylab = "Petal Width (cm)",
     main = "Iris: Petal Length vs Width")
legend("topleft", legend = levels(iris$Species),
       col = 1:3, pch = 16, cex = 0.8)
```

**Species counts:**

```{r}
table(iris$Species)
```
